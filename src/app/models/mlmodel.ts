import { Guid } from 'guid-typescript';

export class MLModel {
    
    created: Date;
    name: string;
    status: string;
    description: string;
    class: string;
    technology: string;
    selected: boolean;
}